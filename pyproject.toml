[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "data-quality-monitor"
version = "0.1.0"
requires-python = ">=3.7"
dependencies = [
    "sqlalchemy>=1.3",
    "pyyaml>=5.3",
    "click>=7.0",
    "requests>=2.24",
]
